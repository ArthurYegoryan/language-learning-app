import "./Header.css";
import HeaderContainer from "./headerContainer/HeaderContainer";

const Header = () => {
    return (
        <header>
            <HeaderContainer />
        </header>
    );
};

export default Header;